name: snowflake-ingest-java
lang: java
lang_version: 8
codeowners:
  enable: true
semaphore:
  enable: true
  pipeline_type: cp
git:
  enable: true
code_artifact:
  enable: true
  package_paths:
    - maven-snapshots/maven/io.confluent/snowflake-ingest-sdk